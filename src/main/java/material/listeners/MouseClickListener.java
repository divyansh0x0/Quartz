package material.listeners;

import java.awt.event.*;

public interface MouseClickListener {
    /***
     * This method is fired when the mouse is clicked inside the component
     * @param e MouseEvent
     */
    void clicked(InputEvent e);
}
